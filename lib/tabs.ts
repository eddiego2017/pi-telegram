/**
 * Telegram legacy tab helper compatibility exports
 * Zones: telegram controls, concurrent workspaces, shared utils
 * Re-exports the workspace helper module under the historical tabs module path during the tab -> workspace rename migration.
 */

export * from "./workspaces.ts";
