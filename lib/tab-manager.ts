/**
 * Telegram legacy tab manager compatibility exports
 * Zones: telegram controls, pi agent, process lifecycle
 * Re-exports the workspace manager module under the historical tab-manager path during the tab -> workspace rename migration.
 */

export * from "./workspace-manager.ts";
