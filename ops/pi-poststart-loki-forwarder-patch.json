{
  "spec": {
    "template": {
      "spec": {
        "containers": [
          {
            "name": "pi",
            "lifecycle": {
              "postStart": {
                "exec": {
                  "command": [
                    "/bin/bash",
                    "-lc",
                    "for i in $(seq 1 60); do tmux has-session -t pi 2>/dev/null && break; sleep 2; done; setsid nohup /home/pi/.local/bin/pi-telegram-loki-forwarder.sh >/dev/null 2>&1 & exit 0"
                  ]
                }
              }
            }
          }
        ]
      }
    }
  }
}
